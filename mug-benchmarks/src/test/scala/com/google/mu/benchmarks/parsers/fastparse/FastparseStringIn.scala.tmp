package com.google.mu.benchmarks.parsers.fastparse

import fastparse._, NoWhitespace._

object FastparseStringIn {
  // Compile-time optimized StringIn parsers
  val fooParser: P[_] => P[Unit] = { implicit ctx: P[_] => StringIn("foobar", "foofoo", "foobaz", "foo", "bar") }
  
  // We will generate the broadParser in the next step or keep it here.
  // Wait, we already generated it in the previous step using python!
  // So we must preserve the broadParser generated by python!
  // Ah! If I use write_to_file, I will overwrite the entire file, which will lose the 676 strings!
  // Oh! That is very important!
  // Yes, if I overwrite the file, I will lose the 676 strings generated by python!
  // So I should use replace_file_content instead of write_to_file to only replace the makeOneOf method!
  // Yes! That is much safer!
}
